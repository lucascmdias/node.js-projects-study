"use strict";

var complain;

class ServerComponent {
  constructor(id, input, out, typeName, customEvents, scope) {
    this.id = id;
    this.Q_ = customEvents;
    this.J_ = scope;
    this.typeName = typeName;
    this.P_ = undefined; // Used to keep track of bubbling DOM events for components rendered on the server
    this._T_ = 0;

    this.onCreate(input, out);
    this._U_ = this.onInput(input, out) || input;
    if (this.S_ === undefined) {
      this.S_ = this._U_;
    }
    this.onRender(out);
  }

  set input(newInput) {
    this.S_ = newInput;
  }

  get input() {
    return this.S_;
  }

  set state(newState) {
    this.L_ = newState;
  }

  get state() {
    return this.L_;
  }

  get _t_() {
    return this.L_;
  }

  elId(nestedId) {
    var id = this.id;

    if (nestedId == null) {
      return id;
    } else {
      if (typeof nestedId !== "string") {

        nestedId = String(nestedId);
        // eslint-disable-next-line no-constant-condition
      }

      if (nestedId.indexOf("#") === 0) {
        id = "#" + id;
        nestedId = nestedId.substring(1);
      }

      return id + "-" + nestedId;
    }
  }

  onCreate() {}
  onInput() {}
  onRender() {}
}

ServerComponent.prototype.getElId = ServerComponent.prototype.elId;

module.exports = ServerComponent;