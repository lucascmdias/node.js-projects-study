var getComponentsContext = require("../ComponentsContext").q_;
var componentsUtil = require("../util");
var componentLookup = componentsUtil.E_;
var registry = require("../registry");
var modernRenderer = require("../renderer");
var resolveComponentKey = modernRenderer.aq_;
var trackAsyncComponents = modernRenderer.aB_;
var beginComponent = require("../beginComponent");
var endComponent = require("../endComponent");
var complain;

function createRendererFunc(templateRenderFunc, componentProps) {
  var typeName = componentProps.e_;
  //var assignedId = componentProps.id;
  var isSplit = componentProps.c_ === true;

  return function renderer(input, out, assignedId, renderingLogic) {
    trackAsyncComponents(out);

    var widgetBody = input.renderBody;
    var widgetState = input.widgetState;
    var widgetConfig = input.widgetConfig;

    var componentsContext = getComponentsContext(out);
    var globalComponentsContext = componentsContext.k_;

    var component = globalComponentsContext._r_;

    var isRerender = component !== undefined;
    var id = assignedId;
    var isExisting;
    var parentComponentDef = componentsContext._M_;
    var ownerComponentDef = out.m_;
    var ownerComponentId = ownerComponentDef && ownerComponentDef.id;
    var key = out.o_;
    var customEvents = out.aC_;

    out.m_ = null;

    if (component) {
      id = component.id;
      isExisting = true;
      globalComponentsContext._r_ = null;
    } else {
      if (key != null) {
        id = id || resolveComponentKey(key.toString(), parentComponentDef);
      } else if (parentComponentDef) {
        id = parentComponentDef._I_();
      } else {
        id = globalComponentsContext._I_();
      }
    }

    if (registry.aD_ && typeName) {
      if (renderingLogic) delete renderingLogic.onRender;
      component = registry._K_(renderingLogic, id, input, out, typeName, customEvents, ownerComponentId);
      if (isSplit || widgetState) {
        component.input = null;
      } else if (input.widgetProps) {
        component.input = input.widgetProps;
        // eslint-disable-next-line no-constant-condition
      }
    } else {
      if (!component) {
        if (isRerender) {
          // Look in in the DOM to see if a component with the same ID and type already exists.
          component = componentLookup[id];
          if (component && component.e_ !== typeName) {
            component = undefined;
          }
        }

        if (component) {
          isExisting = true;
        } else {
          isExisting = false;
          // We need to create a new instance of the component
          if (typeName) {
            component = registry._K_(typeName, id);
          }
        }
      }
    }

    var isFakeComponent = false;

    if (!component) {
      isFakeComponent = true;
      component = {
        id: id,
        p_: {}
      };
    } else {
      component.W_ = true;
    }

    component.state = widgetState;
    component.widgetConfig = widgetConfig;
    component._L_ = widgetBody || component._L_;

    var componentDef = beginComponent(componentsContext, component, key, ownerComponentDef, isSplit, isFakeComponent);
    var parentLegacyComponentDef = componentsContext.aA_;
    componentsContext.aA_ = componentDef;

    // This is a hack, but we have to swap out the component instance stored with this node
    var vComponentNode = out.aE_;

    componentDef.n_ = isFakeComponent ? null : component;
    componentDef._C_ = isExisting;
    componentDef.aj_ = true;

    componentDef.t = function (typeName) {
      if (typeName) {
        if (registry.aD_) {
          var oldComponent = component;
          if (renderingLogic) delete renderingLogic.onRender;
          component = registry._K_(renderingLogic || {}, id, input, out, typeName, customEvents, ownerComponentId);
          if (isSplit || widgetState) {
            component.input = null;
          } else if (input.widgetProps) {
            component.input = input.widgetProps;
            // eslint-disable-next-line no-constant-condition
          }
          Object.assign(component, oldComponent);
          beginComponent(componentsContext, component, key, ownerComponentDef, isSplit, false, this);
        } else {
          vComponentNode.n_ = component = registry._K_(typeName, component.id);
        }
        this.n_ = component;
      }
    };

    if (!isFakeComponent && !registry.aD_) {
      component.F_("aw_");
    }

    // Render the template associated with the component using the final template
    // data that we constructed
    templateRenderFunc(input, out, componentDef, componentDef, component);

    if (customEvents && componentDef.n_) {
      if (registry.aD_) {
        componentDef.Q_ = customEvents;
        componentDef.J_ = ownerComponentId;
      } else {
        componentDef.n_._v_(customEvents, ownerComponentId);
      }
    }

    endComponent(out, componentDef);
    componentsContext._M_ = parentComponentDef;
    componentsContext.aA_ = parentLegacyComponentDef;
  };
}

module.exports = createRendererFunc;